<!DOCTYPE html>
<html>
{{>head}}

<body>
  {{>header}}
  <main>
    <div class="container">
      <div class="row">
        {{#with product}}
        <section class="col-12">
          <h1 class="text-center">{{header}}</h1>
          <p>{{text}}</p>
          {{#each productJumbotron}}
            <div class="row mt-2">
              <div class="jumbotron col-lg-10 col-12 mx-auto m-2 p-1 text-light" id="{{productJumbotronId}}" style="background-image: url(/assets/img/{{image}});">
                <h1 class="">{{title}}</h1>
                <p class="lead">{{textOne}}</p>
                <a class="btn btn-dark btn-lg m-1 text-center" href="{{link}}" role="button">{{linkText}}</a>
              </div>
            </div>
          {{/each}}
        </section>
        {{/with}}
      </div>
    </div>

  </main>


  {{>footer}} {{>scripts}}
</body>

</html>
